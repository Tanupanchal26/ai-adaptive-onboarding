[project]
name = "skillbridge"
version = "2.0.0"
description = "AI Adaptive Onboarding Engine"
requires-python = ">=3.11"

[project.scripts]
app = "streamlit run app.py"

[tool.streamlit]
app = "app.py"
